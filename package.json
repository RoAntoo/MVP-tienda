{
  "name": "ebookspack-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install --include=dev && npm run build && npm run heroku-postbuild",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "cheerio": "^1.2.0"
  }
}
