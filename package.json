{
  "name": "ebookspack-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install && npm run build && npm run heroku-postbuild",
    "start": "cd backend && npm start"
  }
}
